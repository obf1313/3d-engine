/**
 * @description: GLBufferAttribute
 * todo 因为这个全是英文，看不太懂。
 * 这个流属性类不构造顶点缓冲对象，实际上，它使用任何 VBO 是通过在构造函数通过缓冲性能，并且可以在将来改变。
 * 它需要传递除了 VBO 以外的参数。这些是：GL context，GL data type，每个顶点的组件数量，每个组件的字节数量，以及顶点数量。
 * 对于这类最常见的用例是在某种 GPGPU 计算干扰或甚至 pr。
 * @author: cnn
 * @createTime: 2022/1/11 15:52
 **/
import React from 'react';

const GLBufferAttribute = () => {
  return (
    <></>
  );
};
export default GLBufferAttribute;
